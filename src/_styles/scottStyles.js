import { makeStyles } from "@material-ui/core/styles";

const scottStyles = makeStyles({
  
  HeadingText: {
    font: "normal normal bold large/normal Domine, serif",
  },
});

export { scottStyles };
