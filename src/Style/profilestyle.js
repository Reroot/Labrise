import { makeStyles } from "@material-ui/core/styles";

const ProfileStyles = makeStyles({
  textField: {
    margin: ".6rem",
  },
  container: {
    height: "84vh",
  },
});

export { ProfileStyles };
