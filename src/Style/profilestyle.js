import { makeStyles } from "@material-ui/core/styles";

const ProfileStyles = makeStyles({
  textField: {
    outline: "solid",
    outlineColor: "blue",
    margin: ".6rem",
  },
});

export { ProfileStyles };
